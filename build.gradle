buildscript {
  repositories {
    mavenCentral()
  }

  dependencies {
    classpath 'com.android.tools.build:gradle:0.13.0'
  }
}

apply plugin: 'com.android.application'

dependencies {

    // You must install or update the Support Repository through the SDK manager to use this dependency.
    // The Support Repository (separate from the corresponding library) can be found in the Extras category.
    // compile 'com.android.support:support-v4:19.+'
//    compile project(':CropImage')
}

android {
  compileSdkVersion 18
//    buildToolsVersion '19.1.0'
  buildToolsVersion '21.1.1'

/*
sourceSets {
        main {
            manifest.srcFile 'AndroidManifest.xml'
        }
    }
*/

    buildTypes {
        release {
//            runProguard true
//            proguardFiles getDefaultProguardFile('proguard-android.txt'), \
//            'proguard-rules.txt'
        }
    }
}

task launchDebug(type:Exec, dependsOn: 'installDebug'){
    commandLine '/home/massimo/android-sdk-linux/platform-tools/adb', 'shell', 'am', 'start', '-c', 'android.intent.category.LAUNCHER', '-n', 'com.imdp.instaclimb/.CameraActivity'
}

/*
android {
    lintOptions {
        abortOnError false
    }
}

/*
task appo {
  println rootDir
  println rootProject.projectDir
}
*/
