buildscript {
    repositories {
        mavenCentral()
    }

    dependencies {
        classpath 'com.android.tools.build:gradle:0.9.+'
    }
}

apply plugin: 'android'

dependencies {
    compile project(':CropImage')
}

android {
    compileSdkVersion 18
    buildToolsVersion '19.0.3'

/*    sourceSets {
        main {
            manifest.srcFile 'AndroidManifest.xml'
        }
    }
*/

}

task launchDebug(type:Exec, dependsOn: 'installDebug'){
    commandLine '/home/massimo/android-sdks/platform-tools/adb', 'shell', 'am', 'start', '-c', 'android.intent.category.LAUNCHER', '-n', 'com.imdp.instaclimb/.CameraActivity'
}

/*
android {
    lintOptions {
        abortOnError false
    }
}

/*
task appo {
  println rootDir
  println rootProject.projectDir
}
*/
